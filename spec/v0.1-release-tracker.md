# v0.1 — the real first release (tracker)

Status: **active**. Decision (Jack, 2026-07-16): the earlier v0.2/v0.3/v0.4
ladder was ceremony to establish a release cadence; there are no users, so
**collapse everything into a single polished, feature-complete, ready-to-use
`v0.1.0`.** Everything built ships (improve/evals included — no deferral). The
prior version history is discarded, not migrated.

## Phase 1 — version reset & cleanup

- [x] Workspace + crate versions `0.4.0` → `0.1.0` (root Cargo.toml, 7 crate
      path-dep pins, Cargo.lock regenerated; `whip --version` → 0.1.0). 2026-07-16.
- [x] CHANGELOG collapsed to a single `[0.1.0]` entry describing the whole
      feature set; the 0.2/0.3/0.4 sections dropped.
- [x] `spec/release-checklist.md`: the three per-version runbooks replaced with
      one v0.1.0 cut runbook.
- [x] Version references swept (`docs/install.md`, `spec/distribution-tracker.md`,
      `spec/final-audit.md`); no Rust test hardcodes the version.
- [~] Retire old git tags `v0.2.0` / `v0.2.0-mirror` — Jack (remote tag delete
      needs a push; local: `git tag -d`).
- [x] Cleaned stale build artifacts / caches: `clean-rust-caches.sh --aggressive`
      reclaimed ~53GB (this repo's target 36GB→5.4GB) — which also cleared the
      spurious e2e gate failure (a fingerprint-write flake from the full disk).

## Phase 2 — feature completeness ("add all the features")

- [x] Audit that everything built is wired, surfaced, and reachable (4-agent
      audit 2026-07-16). Result: **feature-complete, no hidden gaps** — zero
      real-open build items across all trackers; every subsystem reachable +
      tested. Surface fixes applied: `whip --help` completed (was omitting ~21
      commands), `gov` usage added, `stage-0-skeleton`→`release`, companion skill
      un-staled (queue→std.tracker, dropped `pi`), `check-rule-coverage.sh` fixed
      (4 harness bugs → green), doc version drift swept. **Carried to Phase 4
      docs:** document the 4 built-but-undocumented commands (`stream`,
      `coercion status`, `memory`, `script`) in api-reference.md.
- [x] Reconciled every active tracker (2026-07-16): closed native-provider,
      workflow-composition, tracker-phase-b; expression-kernel gate-complete;
      improve/std-post-campaign/DO active with only deferred-with-cause backlog;
      TRACKERS.md release column swept to v0.1. No silent gaps.

**Honest deferrals for the release notes** (demand-gated / design-pending, not
blockers): DO edge deployment (Cloudflare provisioning) + DO-host egress
allowlist-derivation (documented-only); improve's 6 design residuals;
object-store/versioned-workspace backends; external tracker providers
(GitHub/Linear/Jira); the ingress HTTP listener; coord `acquire … wait`
FIFO/`lease order`; native-OS `bash` (tracked option). **Owner-gated at cut:**
live Codex+Claude strict validation (G-008), publish, retiring old tags.

## Phase 3 — review passes

- [x] Adversarial review (3 parallel agents: security boundaries; tracker/VCS
      correctness; regression of this session's refactors). Boundaries confirmed
      SOUND: Bashkit sandbox, IFC (fail-closed), provider redaction, hosted-exec
      rejection, SSRF pinning/redirect/rebind.
- [x] Fixed (each with a regression test):
      - CRITICAL — tracker merge folded non-causally → cross-clone divergence /
        lost edits; now topological fold, native + DO (2a3b126).
      - HIGH — `import_events` didn't verify the content hash → forge/suppress
        on the shared-folder transport; now rejected + reported (3fea9f4).
      - HIGH — SSRF IPv4-mapped-IPv6 bypass reached IMDS/loopback; both IP
        screens fixed + brought to parity (00cd5e3).
      - MEDIUM — `issue.closed` invisible to the conflict engine (35e524a).
      - MEDIUM — C9 verified-script temp copy TOCTOU → private 0700 dir (479b902).
      - LOW/docs — bash docs described the removed OS-shell model; rewritten to
        the real Bashkit model (da4a373).
- [x] Governance (2 MEDIUM) — taken as documented v0.1 trust assumptions (Jack's
      call): native attestation is a keyless integrity checksum trusting OS file
      perms; the keyed path is the external P-256 verifier; hardening deferred.
      gov.rs doc + spec/information-flow-governance.md corrected (0d8275a).
- [~] Low follow-ups (non-exploitable, deferred cleanup): delete the vestigial
      `bash_allow` field + 14 test call-sites; delete the pre-existing
      `#[cfg(any())]` retired-tool module (~140 lines); document that same-second
      byte-identical independent submissions collapse to one id.

## Phase 4 — docs & companion skill polish (ready-to-use)

- [ ] Quickstart + language reference + operator guide reviewed from a clean
      checkout; a new user can install and run.
- [ ] Companion skill reviewed/updated.
- [ ] Examples all compile + run under the fixture provider.

## Phase 5 — cut v0.1.0

- [ ] Full readiness gate (`WHIPPLESCRIPT_RELEASE_READINESS_FULL=1`) green.
- [ ] Tag `v0.1.0` → release (Jack pushes; tag-push triggers CI).

Related: [[project-release-plan]] (superseded by this), `spec/distribution-tracker.md`,
`spec/release-checklist.md`.

{
  "plugin_id": "legacy-external-notification-package",
  "name": "external-notification",
  "version": "0.1.0",
  "capabilities": [
    {
      "capability": "notify.send",
      "description": "Send an external notification.",
      "schema": {
        "input": {
          "channel": "string",
          "message": "string"
        }
      }
    }
  ],
  "providers": [
    {
      "provider_id": "provider-notify-send",
      "effect_kind": "notify.send",
      "provider": "notification-legacy-provider",
      "capability": "notify.send",
      "config": {}
    }
  ],
  "profiles": [
    {
      "profile_id": "profile-notification-user",
      "name": "notification-user",
      "description": "Allow notification sends.",
      "enforcement_mode": "enforce",
      "allowed_capabilities": ["notify.send"],
      "config": {}
    }
  ],
  "bindings": []
}
